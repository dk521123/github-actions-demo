name: DemoWorkflow

on:
  workflow_dispatch:
jobs:
  demo_job:
    runs-on: ubuntu-latest
    steps:
      - run: |
          version_value=$(cat .github/VERSION)
          echo "version_value: $version_value"
          dasel --version
