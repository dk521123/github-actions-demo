name: DemoAnnotations

on:
  workflow_dispatch:

jobs:
  sample-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Demo
        run: |
           echo '::error file=README.md,line=1,col=3::line and column'
           echo '::warning file=README.md::file name only'
           echo '::notice file=app.js,line=1,col=5,endColumn=7::Missing semicolon'
           echo '::debug::Set the Octocat variable'
        continue-on-error: true
      - name: Demo for Matchers
        run: |
           echo 'main.py:2:3: E111 indentation is not a multiple of four'
           echo 'demo.py:8:20: W292 no newline at end of file'
        continue-on-error: true
