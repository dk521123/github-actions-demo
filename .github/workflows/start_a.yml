name: StartA

on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: echo "Hi!!"      
  call_demo_workflow:
    uses: ./.github/workflows/test_a.yml
    with:
      runs_on: "['ubuntu-24.04']"
